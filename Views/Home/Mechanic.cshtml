<!DOCTYPE html>
<html lang="bg">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Свържете се с механика</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f7fc;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        h1 {
            color: #000;
            margin: 20px;
        }

        

        .contact-form {
            margin-top: 30px;
            padding: 20px;
            background-color: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

            .contact-form input, .contact-form textarea, .contact-form select {
                width: 100%;
                padding: 10px;
                margin: 10px 0;
                border: 1px solid #ccc;
                border-radius: 5px;
                font-size: 1em;
            }

            .contact-form button {
                padding: 10px 20px;
                background-color: #4CAF50;
                color: white;
                border: none;
                border-radius: 5px;
                font-size: 1.2em;
                cursor: pointer;
            }

                .contact-form button:hover {
                    background-color: #45a049;
                }

        .response-message {
            margin-top: 20px;
            font-size: 1.2em;
        }

        /* Фиксиране на футера в дъното */
        footer {
            background-color: #34495e;
            color: #ecf0f1;
            text-align: center;
            padding: 15px 0;
            font-size: 1rem;
            border-top: 1px solid #2c3e50;
            width: 100%;
            position: sticky;
            bottom: 0;
        }

            footer p {
                margin: 0;
                font-size: 1rem;
            }

            footer a {
                color: #ecf0f1;
                text-decoration: none;
            }

                footer a:hover {
                    text-decoration: underline;
                }
    </style>
</head>
<body>
    <h1>Свържете се с механика</h1>

    <div class="content">
        <!-- Контактна форма -->
        <div class="contact-form">
            <h3>Изберете механика и изпратете съобщение</h3>
            <form id="contactForm">
                <!-- Падащо меню за избиране на механик -->
                <label for="mechanic">Изберете механика:</label>
                <select id="mechanic" name="mechanic" required>
                    <option value="gosho_ivanov">Георги Иванов</option>
                    <option value="ivan_petrov">Иван Петров</option>
                </select>

                <label for="name">Вашето име:</label>
                <input type="text" id="name" name="name" required>

                <label for="email">Вашият имейл:</label>
                <input type="email" id="email" name="email" required>

                <label for="message">Вашето съобщение:</label>
                <textarea id="message" name="message" rows="4" required></textarea>

                <button type="submit">Изпрати съобщение</button>
            </form>
        </div>

        <div id="responseMessage" class="response-message"></div>
    </div>

    <script>
        // JavaScript за обработка на формата
        document.getElementById('contactForm').addEventListener('submit', function (event) {
            event.preventDefault(); // Прекратяваме стандартното изпращане на формата

            // Вземаме стойностите от формата
            var mechanic = document.getElementById('mechanic').value;
            var name = document.getElementById('name').value;
            var email = document.getElementById('email').value;
            var message = document.getElementById('message').value;

            // Настройване на мейл адрес и тема
            var subject = 'Запитване за механик: ' + mechanic;
            var body = 'Име: ' + name + '\n' +
                'Имейл: ' + email + '\n\n' +
                'Съобщение:\n' + message;

            // Отваряме мейл клиента с попълнени данни
            var mailtoLink = 'mailto:mechanic@example.com?subject=' + encodeURIComponent(subject) + '&body=' + encodeURIComponent(body);
            window.location.href = mailtoLink;
        });
    </script>
</body>
</html>
